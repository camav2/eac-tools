{
  "name": "eac-tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsc --outDir .test-build && node --test \".test-build/tests/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/node": "^3.0.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "jose": "^4.15.9"
  }
}
